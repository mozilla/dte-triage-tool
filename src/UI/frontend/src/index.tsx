import React from "react";
import ReactDOM from "react-dom";
import KanbanBoard from "./MyComponent";

ReactDOM.render(
  <React.StrictMode>
    <KanbanBoard />
  </React.StrictMode>,
  document.getElementById("root")
);
